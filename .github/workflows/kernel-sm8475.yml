name: Build-sm8475
permissions:
  contents: write
  actions: write

on:
  workflow_call: 
    inputs:
      CPU:
        required: true
        type: string
      FEIL:
        required: false
        type: string
      ANDROID_VERSION:
        required: true
        type: string
      KERNEL_VERSION:
        required: true
        type: string
      KPM:
        required: true
        type: string
      lz4kd:
        required: true
        type: string
      bbr:
        required: true
        type: string
      bbg:
        required: true
        type: string
      proxy:
        required: true
        type: string
      UNICODE_BYPASS:
        required: true
        type: string

jobs:
  build-kernel-sm8475:
    strategy:
      fail-fast: false
      matrix:
        include:
        # - FEIL: 'oneplus_10t'
        # - FEIL: 'oneplus_11r_b'
        - FEIL: 'oneplus_ace2_b'
        # - FEIL: 'oneplus_ace_pro'
        
    uses: ./.github/workflows/Call_Build_SukiSU.yml
    secrets: inherit
    with:
      CPU: ${{ inputs.CPU }}
      FEIL: ${{ matrix.FEIL }}
      ANDROID_VERSION: ${{ inputs.ANDROID_VERSION }}
      KERNEL_VERSION: ${{ inputs.KERNEL_VERSION }}
      SUSFS: ${{ inputs.SUSFS }}
      KPM: ${{ inputs.KPM }}
      lz4kd: ${{ inputs.lz4kd }}
      bbr: ${{ inputs.bbr }}
      bbg: ${{ inputs.bbg }}
      proxy: ${{ inputs.proxy }}
      UNICODE_BYPASS: ${{ inputs.UNICODE_BYPASS }}